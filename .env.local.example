# Local Supabase stack (via `supabase start`).
# For production these values come from the cloud project — they're set on
# Vercel via the infrastructure repo (see infrastructure/exovoid-game.tf).
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key-here
